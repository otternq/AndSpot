<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".PlayFragment" >

    <TextView
        android:id="@+id/spotURL"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Please set your Spot URL in the Settings" />

    <ImageView
        android:id="@+id/playingImg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/spotURL"
        android:layout_alignParentBottom="true"
        android:layout_alignRight="@+id/spotURL"
        android:layout_below="@+id/spotURL"
        android:layout_marginTop="30dp"
        android:src="@drawable/ic_launcher" />

</RelativeLayout>
